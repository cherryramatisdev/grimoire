#!/usr/bin/env perl

use v5.14;

use strict;
use warnings;

my @actions = `php artisan list --format=json | jq -r '.namespaces.[].commands[]'`;

if ($ENV{COMP_LINE}) {
    map {/^$ARGV[1]/ and say $_} grep { index($_, $ARGV[1]) != -1 } @actions;
    exit 0;
}

exec("php artisan @ARGV");
